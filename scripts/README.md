# Scripts

This directory contains scripts used to run computational workflows, reproduce analyses, and generate manuscript results.

Scripts may call functions from the `src/` directory and use configuration files from `configs/`.
