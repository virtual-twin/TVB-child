# Configuration files

This directory contains configuration files defining model parameters, simulation settings, and analysis options used in the TVB-Child workflow.

Configuration files are used to ensure reproducibility and avoid hard-coding parameters within scripts.
